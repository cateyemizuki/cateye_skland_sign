# 森空岛签到插件 — 指令与触发词说明

## 指令一览

| 指令 | 备用写法 | 功能 | 权限 |
|------|---------|------|------|
| `/森空岛` | `/森空岛帮助`、`森空岛` | 固定回复：token 获取教程（含链接）+ 指令列表 | 所有人 |
| `/森空岛绑定 <token>` | `森空岛绑定 <token>` | 绑定 token | 所有人 |
| `/森空岛解绑` | `森空岛解绑` | 解绑并停止自动签到 | 所有人 |
| `/森空岛签到` | `森空岛签到` | 手动签到 | 所有人 |
| `/森空岛状态` | `森空岛状态` | 查看今日签到状态 | 所有人 |
| `/森空岛自动签到` | `森空岛自动签到` | 开启 / 关闭自动签到 | 所有人 |
| `/森空岛定时 <时间>` | `森空岛定时 6点30分` | 设置自动签到时间 | 仅超级管理员 |
| `/森空岛签到信息` | `森空岛签到信息` | 查看所有用户签到状态（合并转发） | 仅超级管理员 |
| `/森空岛自动签到测试` | `森空岛自动签到测试` | 立即执行一次自动签到 | 仅超级管理员 |

所有指令均支持带 `/` 或不带 `/` 两种写法。

## token 输入格式

`/森空岛绑定` 支持两种 token 格式（`extract_token_from_text` 自动识别）：

1. 完整 JSON：

   ```
   {"code":0,"data":{"content":"请复制这一长串字符"}}
   ```

2. 纯 Base64 字符串：

   ```
   dXNlci10b2tlbi1zdHJpbmc=
   ```

## 触发词（自然语言 / LLM 工具）

| 工具 | 触发场景示例 | 参数 |
|------|-------------|------|
| `skland_help` | 「森空岛怎么用」「怎么获取 token」「如何绑定」 | 无（返回 token 获取链接与作用，由模型组织回复） |
| `skland_bind_token` | 「绑定 token：xxx」「帮我开通森空岛签到」 | `user_id`、`token` |
| `skland_sign_in` | 「帮我签到」「森空岛签到」 | `user_id` |
| `skland_sign_status` | 「我今天签到了吗」「查一下签到状态」 | `user_id` |
| `skland_auto_sign` | 「帮我开启自动签到」「关掉自动签到」 | `user_id`、`enable` |

> 工具仅「明确要求」时调用；只分享链接不提要求则不调用（见 Tool description 约束）。

## token 获取链接约定

token 获取采用**链接方案**（不再使用二维码）：

- 获取链接：`https://www.skland.com/article?id=6216915&c_c=COPY`
- 作用：用浏览器打开该链接会跳转到森空岛登录页，登录后点击帖子中的链接即可获取绑定 token。
- **可配置**：链接存放在配置 `[token] get_url` 中，默认使用上述链接，用户可在 WebUI / `config.toml` 中自行修改；`skland_help` 工具与 `/森空岛` 指令均使用该配置值。

调用方式：

- **LLM 工具**：调用 `skland_help` 时，插件会**独立向当前对话发送一条只含该链接的消息**（无其他文字）；同时把链接、链接作用与获取步骤返回给大模型，**由大模型自行构建回答**。
- **指令**：`/森空岛`（或 `/森空岛帮助`）触发时**固定回复**完整获取教程（含链接与指令列表）。
